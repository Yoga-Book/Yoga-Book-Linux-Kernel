version: '3'

vars:
  JOBS:
    sh: nproc

tasks:
  default:
    desc: Configure and build Yoga Book Debian kernel packages
    cmds:
      - task: build

  build:dev:
    desc: Build timestamped Yoga Book development Debian packages
    cmds:
      - task: build

  build:prod:
    desc: Build final Yoga Book production Debian packages without a timestamp
    cmds:
      - task: build
        vars:
          FINAL: 'true'

  build:setup:
    desc: Install kernel build tools on Ubuntu 24.04 or 26.04
    silent: true
    cmds:
      - |
        if [ ! -r /etc/os-release ]; then
          echo "Cannot identify the operating system: /etc/os-release is unavailable" >&2
          exit 1
        fi

        . /etc/os-release
        if [ "${ID:-}" != ubuntu ]; then
          echo "Unsupported operating system: ${PRETTY_NAME:-unknown}" >&2
          echo "build:setup supports Ubuntu 24.04 and Ubuntu 26.04 only" >&2
          exit 1
        fi

        case "${VERSION_ID:-}" in
          24.04)
            ubuntu_release="Ubuntu 24.04 LTS"
            ;;
          26.04)
            ubuntu_release="Ubuntu 26.04 LTS"
            ;;
          *)
            echo "Unsupported Ubuntu version: ${VERSION_ID:-unknown}" >&2
            echo "build:setup supports Ubuntu 24.04 and Ubuntu 26.04 only" >&2
            exit 1
            ;;
        esac

        set -- \
          bc bison build-essential ccache cpio debhelper dwarves fakeroot flex \
          git kmod libdw-dev libelf-dev libssl-dev python3 rsync zstd

        echo "Configuring the kernel build tools for $ubuntu_release"
        if [ "$(id -u)" -eq 0 ]; then
          apt-get update
          apt-get install --yes --no-install-recommends "$@"
        elif command -v sudo >/dev/null 2>&1; then
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends "$@"
        else
          echo "sudo is required to install the kernel build tools" >&2
          exit 1
        fi

  config:refresh:
    desc: Refresh the versioned config from the pinned official Ubuntu generic config
    cmds:
      - scripts/yogabook/refresh-config.sh

  configure:
    desc: Prepare .config from the versioned Yoga Book defconfig
    silent: true
    env:
      YOGABOOK_FINAL: '{{.FINAL | default "false"}}'
    cmds:
      - |
        case "$YOGABOOK_FINAL" in
          true)
            local_version="-yogabook"
            ;;
          false)
            build_timestamp="$(date +%Y%m%d-%H%M%S)"
            local_version="-yogabook-$build_timestamp"
            ;;
          *)
            echo "FINAL must be true or false" >&2
            exit 1
            ;;
        esac

        make yogabook_x91l_defconfig
        # Prefer SOF now that the Yoga Book has an IPC3 topology. Keep the
        # legacy SST ACPI frontend available for explicit fallback testing.
        scripts/config --module SND_SST_ATOM_HIFI2_PLATFORM_ACPI
        scripts/config --module SND_SOC_SOF_BAYTRAIL
        scripts/config --enable SND_INTEL_BYT_PREFER_SOF
        scripts/config --set-str SYSTEM_TRUSTED_KEYS ""
        scripts/config --set-str SYSTEM_REVOCATION_KEYS ""
        scripts/config --set-str LOCALVERSION "$local_version"
        scripts/config --disable LOCALVERSION_AUTO
        make olddefconfig
        make syncconfig

        scripts/yogabook/check-config.sh .config

        kernel_release="$(make -s LOCALVERSION= kernelrelease)"
        expected_release="7.2.0$local_version"
        if [ "$kernel_release" != "$expected_release" ]; then
          echo "Unexpected kernel release: $kernel_release (expected $expected_release)" >&2
          exit 1
        fi
        echo "Kernel release: $kernel_release"

  build:
    desc: Build Yoga Book image and headers Debian packages
    silent: true
    deps:
      - task: configure
        vars:
          FINAL: '{{.FINAL | default "false"}}'
    env:
      YOGABOOK_JOBS: '{{.JOBS}}'
    cmds:
      - |
        case "$YOGABOOK_JOBS" in
          ''|0|*[!0-9]*)
            echo "JOBS must be a positive integer" >&2
            exit 1
            ;;
        esac

        PATH="/usr/lib/ccache:$PATH" \
          make -j"$YOGABOOK_JOBS" \
          CC=gcc LOCALVERSION= bindeb-pkg
